import ChatList from "@/components/ChatList";
import ScreenWrapper from "@/components/ScreenWrapper";

const Home = () => {
  return (
    <ScreenWrapper>
      <ChatList />
    </ScreenWrapper>
  );
};

export default Home;
